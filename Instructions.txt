You will be creating a complete HTML presentation with embedded CSS styling and JavaScript functionality based on a script. The presentation should be optimized for recording, with minimal text and maximum visual impact.

Here is the script for the presentation:

<script>
{{SCRIPT}}
</script>

Here is the presenter information to include on the title slide:

<presenter_info>
Name: {{PRESENTER_NAME}}
Email: {{PRESENTER_EMAIL}}
Nostr: {{PRESENTER_NOSTR}}
</presenter_info>

<presentation_metadata>
Title: {{PRESENTATION_TITLE}}
Date: {{PRESENTATION_DATE}}
</presentation_metadata>

Your task is to create a single HTML file that contains a complete slide presentation based on the script provided above.

## Content Requirements:
- Create a title slide that includes the presentation title, date, and the presenter's name, email address, and nostr pub
- Each slide should contain minimal text - focus on key numbers, statistics, or single important phrases
- Prioritize visual elements: diagrams, charts, icons, illustrations, or large impactful numbers
- Extract the main chapters/sections from the script to create a logical flow of slides
- Each major point should get its own slide rather than cramming multiple points together
- Use the script to determine what content belongs on each slide, but present it visually rather than as paragraphs of text

## Visual Design Requirements:
- Use high contrast color schemes (dark background with light text works well for recording)
- Large, readable fonts for any text that appears
- Clean, minimal design with plenty of white space
- Visual hierarchy should guide the viewer's attention to what matters most
- Use CSS to create simple diagrams, charts, or visual representations where appropriate
- Consider using CSS shapes, gradients, and animations to make slides more engaging
- Each slide should be visually distinct and memorable

## Technical Requirements:
- Create a single, self-contained HTML file with embedded CSS (in <style> tags) and JavaScript (in <script> tags)
- Implement keyboard navigation: left/right arrow keys to navigate between slides
- Display current slide number and total slides in the top right corner (e.g., "3/12")
- Show a chapter navigation menu on the left side of the screen listing all major sections
- Highlight the current chapter in the navigation menu as the user progresses through slides
- Ensure smooth transitions between slides (CSS transitions recommended)
- Make the presentation fullscreen-friendly for recording
- The presentation should work when opened directly in a browser without requiring a server

## Navigation Structure:
- The left sidebar should show chapter names extracted from the script
- Clicking on a chapter name should jump to the first slide of that section
- The current chapter should be visually highlighted in the navigation menu
- Slides should be grouped logically under their respective chapters
- The navigation should update automatically as the user moves through slides

Before creating the HTML file, use the scratchpad below to plan your approach:

<scratchpad>
Analyze the script and plan out:
1. What are the main chapters/sections from the script? List them.
2. How many slides will you need for each chapter?
3. What key visual elements can you create for each major point? (Think: charts, diagrams, large numbers, icons)
4. What specific numbers, statistics, or key phrases should be highlighted on individual slides?
5. How will you structure the slide progression to match the flow of the script?
6. What color scheme and visual style will you use?
</scratchpad>

Now create the complete HTML presentation. Your output should be a single, complete HTML file that includes:
- Complete HTML structure with DOCTYPE
- All CSS embedded in <style> tags in the <head>
- All JavaScript embedded in <script> tags before the closing </body> tag
- All slides structured as individual div elements with a common class
- The chapter navigation sidebar
- The slide counter
- Keyboard navigation functionality
- Smooth transitions between slides

The HTML file should be ready to open directly in a browser for recording the presentation. Make sure the presentation flows logically, each slide serves a clear purpose in supporting the script, and the visual design is optimized for recording with high contrast and minimal text.

Your entire response should be the complete HTML file code, ready to be saved as an .html file and opened in a browser. Do not include any explanatory text before or after the HTML code - only output the HTML file itself.